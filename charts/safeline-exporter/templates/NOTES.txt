SafeLine Exporter has been installed.

{{- if not .Values.safeline.address }}

WARNING: safeline.address is empty. Set it to the SafeLine base URL before the
Deployment starts.
{{- end }}
{{- if and (not .Values.existingSecret) (not .Values.secret.create) }}

WARNING: no API-token Secret is configured. Set existingSecret to a Secret that
contains key {{ .Values.existingSecretKey | quote }}, or enable secret.create.
{{- end }}

Metrics endpoint inside the cluster:
  http://{{ include "safeline-exporter.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}{{ .Values.exporter.metricsPath }}

To test with port-forwarding:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "safeline-exporter.fullname" . }} 9719:{{ .Values.service.port }}
  curl http://127.0.0.1:9719{{ .Values.exporter.metricsPath }}

{{- if not .Values.serviceMonitor.enabled }}
ServiceMonitor is disabled. Enable serviceMonitor.enabled when the Prometheus
Operator CRDs are installed.
{{- end }}
